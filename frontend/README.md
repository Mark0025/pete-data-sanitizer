## frontend/

This folder is reserved for a future UI (if/when needed).

### Current status
We do **not** have a separate JavaScript frontend today.

The current UI is a minimal FastAPI server (`python -m pete_dm_clean serve`) that returns HTML/plain text and uses a small amount of client-side JS for diagram rendering.

